[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — serve index.html for all routes so React Router works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
